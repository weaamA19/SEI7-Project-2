<main>

<div class="album py-5 bg-body-tertiary">
<div class="container">
    
    <h1><%= title %> <span class="text-white"><%= recipe.recipeName %></span></h1>

<div class="row g-5">
    <div class="col-md-7 col-lg-8">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <span>Created: <%= dayjs(recipe.createdAt).format('DD/MM/YYYY') %> | Updated: <%= dayjs(recipe.updatedAt).fromNow(true) %> ago</span>
            <span>Featured: <%= recipe.isPublished %></span>
        </div>
        <p><%= recipe.content %></p>
        <hr>
        <p><%- recipe.ingredient %></p>
    </div>
    <div class="col-md-5 col-lg-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <span>Preparation Time</span>
            <span class="badge bg-success rounded-pill"><%= recipe.duration %> mins</span>
        </div>
        <p><% let thePhoto = "empty.jpg"; if(recipe.thumbnail){ thePhoto = recipe.thumbnail } %><img src="../images/<%= thePhoto %>" width="100%"></p>
    </div>
</div>

</div>
</div>

</main>